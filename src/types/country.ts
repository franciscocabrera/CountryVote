export type Country = {
  name: string,
  official_name: string,
  capital_city: string,
  region: string,
  sub_region: string,
  votes: number
}

export type CountryTemperature = {
  name: string
  temp: number
  description: string
}